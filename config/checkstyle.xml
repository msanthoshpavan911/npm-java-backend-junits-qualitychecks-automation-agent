<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- Checker-level checks (whole-file scope) -->
    <module name="LineLength">
        <property name="max" value="150"/>
        <property name="ignorePattern" value="^package.*|^import.*|http://|https://"/>
    </module>

    <module name="FileLength">
        <property name="max" value="500"/>
    </module>

    <!-- Reject tab characters — code must use spaces for indentation -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- AST-level checks -->
    <module name="TreeWalker">

        <module name="UnusedImports"/>

        <module name="LocalVariableName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]{0,39}$"/>
        </module>

        <module name="ParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]{0,39}$"/>
        </module>

        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected|ignore|ignored|_"/>
        </module>

        <module name="MethodLength">
            <property name="max" value="80"/>
        </module>

    </module>
</module>
